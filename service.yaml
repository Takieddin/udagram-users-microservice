apiVersion: v1
kind: Service
metadata:
  name: udagram-user-node-udacity
  labels:
    run: udagram-user-node-udacity
spec:
  ports:
  - port: 80
    protocol: TCP
  selector:
    run: udagram-user-node-udacity